import Rx from 'rxjs'


function create({state$}) {
  // TODO: state$ buffer of PARAM_1 amount.
  // TODO: set state hash as id for traversal and validation of state.
  // TODO: action function that takes a hash
  // and produces an event with the resolved state as data.
}

export default {
  create
}
